# Linux Commands

1. rg: recursively searches the current directory for a regrex pattern
2. file <filename>: check file type
3. du -sh <path>: check directory storage size
4. df -h <path>: display information about total space and available space on a file system
